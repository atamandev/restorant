# 🔗 لینک‌های Localhost

## 🚀 بعد از اجرای `npm run dev`:

### 📱 **صفحه سفارش‌دهی مشتریان (QR Code)**
```
http://localhost:3000/order
```
**توضیح**: صفحه سفارش آنلاین برای مشتریان - بدون نیاز به لاگین

---

### 🌐 **سایت اصلی (Dashboard)**
```
http://localhost:3000/
```
**توضیح**: داشبورد مدیریت رستوران - نیاز به لاگین دارد

---

### 🔐 **صفحه ورود**
```
http://localhost:3000/login
```
**توضیح**: ورود به پنل مدیریت

**اطلاعات ورود پیش‌فرض:**
- نام کاربری: `admin`
- رمز عبور: `123456`

---

## 📋 سایر لینک‌های مهم:

### صفحات اصلی:
- Dashboard: `http://localhost:3000/`
- POS: `http://localhost:3000/pos`
- Accounting: `http://localhost:3000/accounting`
- Inventory: `http://localhost:3000/inventory`
- Customers: `http://localhost:3000/customers`
- Reports: `http://localhost:3000/reports`
- Settings: `http://localhost:3000/settings`

### API Endpoints:
- Menu Items: `http://localhost:3000/api/menu-items`
- Orders: `http://localhost:3000/api/orders`
- Table Orders: `http://localhost:3000/api/table-orders`
- Restaurant Settings: `http://localhost:3000/api/restaurant-settings`

---

## 💡 نکات:

1. **پورت پیش‌فرض**: `3000`
2. **اگر پورت تغییر کرد**: متغیر محیطی `PORT` را تنظیم کنید
3. **برای نمایش لینک‌ها**: `npm run show-urls` را اجرا کنید

