# 📚 توضیح کامل سیستم مدیریت رستوران

## 🎯 معرفی کلی

این سیستم یک **سیستم مدیریت رستوران حرفه‌ای** است که با **Next.js 14** و **TypeScript** ساخته شده و از **MongoDB** به عنوان دیتابیس استفاده می‌کند. این سیستم تمامی عملیات یک رستوران را از ثبت سفارش تا حسابداری و گزارش‌گیری مدیریت می‌کند.

---

## 🏗️ معماری سیستم

### ساختار کلی:
```
Frontend (Next.js 14 + React)
    ↓
API Routes (Next.js API Routes)
    ↓
MongoDB (Database)
```

### تکنولوژی‌های استفاده شده:
- **Frontend**: Next.js 14, React, TypeScript, Tailwind CSS
- **Backend**: Next.js API Routes (Serverless Functions)
- **Database**: MongoDB
- **Icons**: Lucide React
- **Charts**: Chart.js (برای نمودارها)

---

## 📦 ماژول‌های اصلی سیستم

### 1️⃣ **Setup & Onboarding (راه‌اندازی اولیه)**

**هدف**: تنظیم اطلاعات پایه رستوران قبل از شروع کار

**صفحات**:
- `/onboarding/setup-branch` - تعریف شعبه و صندوق‌ها
- `/onboarding/menu-setup` - تعریف منو و کالاها
- `/onboarding/people-setup` - تعریف مشتریان، تامین‌کنندگان و کارکنان
- `/onboarding/initial-inventory` - ثبت موجودی اولیه انبار
- `/onboarding/tax-settings` - تنظیمات مالیات و کارمزد

**نحوه کار**:
1. کاربر اطلاعات پایه را وارد می‌کند (شعبه، منو، اشخاص، موجودی)
2. داده‌ها در MongoDB ذخیره می‌شوند
3. سیستم آماده استفاده از POS و سایر ماژول‌ها می‌شود

**Collections استفاده شده**:
- `branches` - اطلاعات شعبه‌ها
- `cash_registers` - اطلاعات صندوق‌ها
- `menu_items` - آیتم‌های منو
- `inventory_items` - مواد اولیه
- `people` - مشتریان و تامین‌کنندگان
- `staff` - کارکنان
- `warehouses` - انبارها
- `inventory_balance` - موجودی واقعی

---

### 2️⃣ **POS (Point of Sale - سیستم فروش)**

**هدف**: ثبت و مدیریت سفارشات مشتریان

**صفحات**:
- `/pos/dine-in` - سفارشات حضوری (میز)
- `/pos/takeaway` - سفارشات بیرون‌بر
- `/pos/delivery` - سفارشات ارسال
- `/operations/table-order` - ثبت سفارش میز
- `/operations/quick-sale` - فروش سریع

**جریان کار کامل**:

```
1. ثبت سفارش
   ├─ کاربر منو را انتخاب می‌کند
   ├─ میز/مشتری را انتخاب می‌کند
   └─ سفارش را ثبت می‌کند
   
2. API: POST /api/dine-in-orders (یا takeaway/delivery)
   ├─ سفارش در MongoDB ذخیره می‌شود
   ├─ status = 'pending'
   └─ orderNumber تولید می‌شود
   
3. ارسال به آشپزخانه
   ├─ Kitchen Order ساخته می‌شود
   ├─ status = 'preparing'
   └─ آشپز سفارش را می‌بیند
   
4. رزرو موجودی
   ├─ موجودی مواد اولیه رزرو می‌شود
   ├─ در inventory_reservations ثبت می‌شود
   └─ موجودی واقعی هنوز کم نشده
   
5. آماده‌سازی غذا
   ├─ آشپز غذا را آماده می‌کند
   ├─ status = 'ready'
   └─ POS می‌بیند غذا آماده است
   
6. پرداخت
   ├─ مشتری پرداخت می‌کند
   ├─ status = 'paid'
   └─ Trigger می‌شود:
       ├─ Accounting: Invoice ساخته می‌شود
       ├─ Inventory: موجودی واقعی کم می‌شود
       └─ Customers: امتیاز وفاداری افزایش می‌یابد
```

**Collections استفاده شده**:
- `dine_in_orders` - سفارشات حضوری
- `takeaway_orders` - سفارشات بیرون‌بر
- `delivery_orders` - سفارشات ارسال
- `table_orders` - سفارشات میز
- `orders` - سفارشات عمومی (برای گزارشات)
- `kitchen_orders` - سفارشات آشپزخانه

---

### 3️⃣ **Kitchen (آشپزخانه)**

**هدف**: مدیریت سفارشات آشپزخانه و آماده‌سازی غذا

**صفحات**:
- `/pos/kitchen-orders` - لیست سفارشات آشپزخانه

**جریان کار**:
1. سفارش از POS به آشپزخانه ارسال می‌شود
2. آشپز سفارش را می‌بیند و شروع به پخت می‌کند
3. وقتی غذا آماده شد، status را به 'ready' تغییر می‌دهد
4. POS مطلع می‌شود و به مشتری اطلاع می‌دهد

**Collections استفاده شده**:
- `kitchen_orders` - سفارشات آشپزخانه

---

### 4️⃣ **Inventory (انبارداری)**

**هدف**: مدیریت موجودی مواد اولیه و کالاها

**صفحات**:
- `/onboarding/initial-inventory` - ثبت موجودی اولیه
- `/inventory/warehouses` - مدیریت انبارها
- `/inventory/warehouses/[id]` - جزئیات انبار
- `/inventory/transfers` - انتقال بین انبارها
- `/inventory/stock-alerts` - هشدارهای موجودی
- `/inventory/reports` - گزارشات موجودی
- `/inventory/item-ledger` - دفتر کل موجودی
- `/inventory/audit` - انبارگردانی

**اصول کلیدی**:

#### 1. منبع حقیقت موجودی
- موجودی واقعی در `inventory_balance` نگهداری می‌شود
- هر رکورد = یک کالا × یک انبار
- `inventory_items.currentStock` فقط برای نمایش است و از Balance محاسبه می‌شود

#### 2. سیستم رزرو موجودی
- وقتی سفارش ثبت می‌شود، موجودی **رزرو** می‌شود (در `inventory_reservations`)
- موجودی واقعی هنوز کم نشده
- وقتی سفارش پرداخت شد، موجودی رزرو شده **مصرف** می‌شود و موجودی واقعی کم می‌شود

#### 3. انواع حرکت کالا
- `INITIAL` - موجودی اولیه
- `PURCHASE_IN` - ورود خرید
- `SALE_CONSUMPTION` - مصرف فروش
- `TRANSFER_OUT` - خروج انتقال
- `TRANSFER_IN` - ورود انتقال
- `ADJUSTMENT_INCREMENT` - اصلاح افزایشی
- `ADJUSTMENT_DECREMENT` - اصلاح کاهشی
- `WASTAGE` - ضایعات

#### 4. FIFO (First In First Out)
- هر ورود کالا یک لایه FIFO ایجاد می‌کند
- مصرف‌ها از قدیمی‌ترین لایه‌ها تخلیه می‌شوند
- برای محاسبه دقیق قیمت تمام‌شده

#### 5. هشدارهای موجودی
- `out_of_stock` - موجودی صفر
- `low_stock` - موجودی کمتر از حداقل
- `overstock` - موجودی بیشتر از حداکثر
- `expiry` - نزدیک به انقضا

**جریان کار**:
```
1. ثبت موجودی اولیه
   └─ در inventory_balance ثبت می‌شود
   
2. خرید مواد اولیه
   ├─ Purchase ثبت می‌شود
   ├─ Stock Movement: PURCHASE_IN
   └─ inventory_balance افزایش می‌یابد
   
3. ثبت سفارش
   ├─ موجودی رزرو می‌شود (inventory_reservations)
   └─ موجودی واقعی هنوز کم نشده
   
4. پرداخت سفارش
   ├─ موجودی رزرو شده مصرف می‌شود
   ├─ Stock Movement: SALE_CONSUMPTION
   └─ inventory_balance کاهش می‌یابد
   
5. بررسی هشدارها
   ├─ اگر موجودی < minStock → low_stock alert
   └─ اگر موجودی = 0 → out_of_stock alert
```

**Collections استفاده شده**:
- `inventory_items` - اطلاعات کالاها
- `inventory_balance` - موجودی واقعی (item × warehouse)
- `inventory_reservations` - موجودی رزرو شده
- `stock_movements` - حرکات کالا
- `stock_alerts` - هشدارهای موجودی
- `item_ledger` - دفتر کل موجودی
- `fifo_layers` - لایه‌های FIFO
- `warehouses` - انبارها
- `transfers` - انتقالات بین انبارها

---

### 5️⃣ **Accounting (حسابداری)**

**هدف**: مدیریت مالی و حسابداری رستوران

**صفحات**:
- `/accounting/invoices` - فاکتورهای فروش و خرید
- `/accounting/purchases` - خریدها
- `/accounting/receipts-payments` - دریافت و پرداخت
- `/accounting/bank-accounts` - حساب‌های بانکی
- `/accounting/cheques` - چک‌ها
- `/accounting/cash-flow` - جریان نقدی
- `/accounting/balance-sheet` - ترازنامه
- `/accounting/ledgers` - دفاتر مالی
- `/accounting/cash-drawers` - مدیریت صندوق‌ها
- `/operations/close-cashier` - بستن صندوق

**جریان کار**:

#### ثبت فاکتور فروش:
```
1. سفارش پرداخت می‌شود (status = 'paid')
   ↓
2. Invoice خودکار ساخته می‌شود:
   ├─ type = 'sales'
   ├─ status = 'paid'
   ├─ totalAmount = order.total
   └─ customerId = order.customerId
   ↓
3. Receipt ثبت می‌شود:
   ├─ type = 'receipt'
   ├─ method = order.paymentMethod
   └─ amount = order.total
   ↓
4. Cashier Session به‌روزرسانی می‌شود:
   ├─ totalSales += order.total
   ├─ cashSales += (if cash)
   └─ cardSales += (if card)
```

#### ثبت خرید:
```
1. کاربر Purchase ثبت می‌کند
   ↓
2. Invoice ساخته می‌شود:
   ├─ type = 'purchase'
   └─ supplierId = purchase.supplierId
   ↓
3. Payment ثبت می‌شود:
   ├─ type = 'payment'
   └─ amount = purchase.total
   ↓
4. Inventory به‌روزرسانی می‌شود:
   └─ Stock Movement: PURCHASE_IN
```

**Collections استفاده شده**:
- `invoices` - فاکتورها (فروش و خرید)
- `purchases` - خریدها
- `receipts_payments` - دریافت و پرداخت
- `bank_accounts` - حساب‌های بانکی
- `cheques` - چک‌ها
- `cash_flow` - جریان نقدی
- `cashier_sessions` - جلسات صندوق
- `financial_reports_pnl` - گزارشات سود و زیان

---

### 6️⃣ **Customers (CRM - مدیریت ارتباط با مشتری)**

**هدف**: مدیریت اطلاعات مشتریان و برنامه وفاداری

**صفحات**:
- `/customers` - لیست مشتریان
- `/customers/add` - افزودن مشتری
- `/customers/loyalty` - برنامه وفاداری
- `/customers/feedback` - نظرات مشتریان

**جریان کار**:

#### برنامه وفاداری:
```
1. مشتری سفارش می‌دهد و پرداخت می‌کند
   ↓
2. امتیاز محاسبه می‌شود:
   ├─ pointsEarned = order.total * 0.01 (1% از مبلغ)
   └─ totalPoints += pointsEarned
   ↓
3. Tier بررسی می‌شود:
   ├─ اگر totalPoints > 10000 → Gold
   ├─ اگر totalPoints > 5000 → Silver
   └─ در غیر این صورت → Bronze
   ↓
4. مشتری می‌تواند از امتیازها استفاده کند
```

**Collections استفاده شده**:
- `customers` - اطلاعات مشتریان
- `customer_loyalties` - برنامه وفاداری
- `customer_feedback` - نظرات مشتریان

---

### 7️⃣ **Reports (گزارشات)**

**هدف**: تحلیل و گزارش‌گیری از تمام بخش‌ها

**صفحات**:
- `/` (Dashboard) - داشبورد مدیریتی
- `/reports/sales` - گزارشات فروش
- `/reports/inventory` - گزارشات موجودی
- `/reports/financial` - گزارشات مالی
- `/reports/customer-supplier` - گزارشات مشتری/تامین‌کننده
- `/reports/general` - گزارشات عمومی

**انواع گزارشات**:

#### گزارشات فروش:
- فروش روزانه/هفتگی/ماهانه
- فروش بر اساس نوع سفارش (حضوری/بیرون‌بر/ارسال)
- فروش بر اساس منو
- فروش بر اساس روش پرداخت

#### گزارشات موجودی:
- سطح موجودی (Stock Level)
- ارزش موجودی (Valuation)
- گردش موجودی (Turnover)
- موجودی منقضی (Expiry)
- هشدارهای موجودی (Alerts)

#### گزارشات مالی:
- سود و زیان (P&L)
- ترازنامه (Balance Sheet)
- جریان نقدی (Cash Flow)
- گزارشات چک و پرداخت

**Collections استفاده شده**:
- همه Collections برای جمع‌آوری داده

---

### 8️⃣ **Operations (عملیات روزانه)**

**هدف**: عملیات روزانه رستوران

**صفحات**:
- `/operations/quick-sale` - فروش سریع
- `/operations/table-order` - ثبت سفارش میز
- `/operations/close-cashier` - بستن صندوق
- `/operations/daily-report` - گزارش روزانه
- `/orders/pending` - سفارشات در انتظار
- `/orders/preparing` - سفارشات در حال آماده‌سازی
- `/orders/ready` - سفارشات آماده
- `/orders/completed` - سفارشات تکمیل شده

**جریان بستن صندوق**:
```
1. کاربر جلسه صندوق را باز می‌کند
   ├─ startAmount = موجودی اولیه
   └─ status = 'open'
   
2. در طول روز:
   ├─ سفارشات ثبت می‌شوند
   ├─ پرداخت‌ها انجام می‌شوند
   └─ cashier_sessions به‌روزرسانی می‌شود
   
3. در پایان روز:
   ├─ کاربر endAmount را وارد می‌کند
   ├─ difference = endAmount - expectedCash
   └─ status = 'closed'
   
4. گزارش بستن صندوق:
   ├─ کل فروش
   ├─ فروش نقدی/کارتی
   ├─ تعداد تراکنش‌ها
   └─ تفاوت موجودی
```

---

### 9️⃣ **Settings (تنظیمات)**

**هدف**: تنظیمات سیستم و مدیریت کاربران

**صفحات**:
- `/settings/restaurant` - تنظیمات رستوران
- `/settings/staff` - مدیریت کارکنان
- `/settings/user-roles` - سطح دسترسی
- `/settings/printer-config` - تنظیمات چاپگر
- `/settings/invoice-templates` - قالب فاکتور
- `/settings/backup-restore` - پشتیبان‌گیری
- `/settings/audit-log` - لاگ فعالیت‌ها
- `/settings/notifications` - اعلان‌ها
- `/settings/help` - راهنما

---

## 🔄 جریان داده‌ها بین ماژول‌ها

### مثال کامل: فروش یک پیتزا

```
1. Setup (قبل از شروع)
   ├─ منو: "پیتزای مخصوص" تعریف شده
   ├─ مواد اولیه: آرد، پنیر، سس در انبار موجود است
   └─ مشتری: "احمد محمدی" در سیستم ثبت شده
   
2. POS - ثبت سفارش
   ├─ کاربر: "پیتزای مخصوص" را انتخاب می‌کند
   ├─ میز: "میز 5" را انتخاب می‌کند
   └─ API: POST /api/dine-in-orders
       ├─ سفارش در dine_in_orders ذخیره می‌شود
       ├─ status = 'pending'
       └─ orderNumber = 'DI-001'
   
3. Kitchen - ارسال به آشپزخانه
   ├─ API: Kitchen Order ساخته می‌شود
   ├─ status = 'preparing'
   └─ آشپز سفارش را می‌بیند
   
4. Inventory - رزرو موجودی
   ├─ API: موجودی مواد اولیه رزرو می‌شود
   ├─ در inventory_reservations ثبت می‌شود:
   │   ├─ آرد: 200 گرم (رزرو)
   │   ├─ پنیر: 100 گرم (رزرو)
   │   └─ سس: 50 گرم (رزرو)
   └─ موجودی واقعی هنوز کم نشده
   
5. Kitchen - آماده‌سازی
   ├─ آشپز غذا را آماده می‌کند
   ├─ API: PUT /api/kitchen-orders/{id}
   └─ status = 'ready'
   
6. POS - پرداخت
   ├─ مشتری پرداخت می‌کند (نقدی)
   ├─ API: PUT /api/dine-in-orders/{id}
   └─ status = 'paid'
   
7. Accounting - ثبت فاکتور
   ├─ API: Invoice خودکار ساخته می‌شود
   ├─ type = 'sales'
   ├─ totalAmount = 150,000 تومان
   └─ در invoices ذخیره می‌شود
   
8. Inventory - مصرف موجودی
   ├─ API: موجودی رزرو شده مصرف می‌شود
   ├─ Stock Movement: SALE_CONSUMPTION
   └─ inventory_balance کاهش می‌یابد:
       ├─ آرد: -200 گرم
       ├─ پنیر: -100 گرم
       └─ سس: -50 گرم
   
9. Customers - به‌روزرسانی امتیاز
   ├─ API: امتیاز محاسبه می‌شود
   ├─ pointsEarned = 1500 (1% از 150,000)
   └─ customer_loyalties به‌روزرسانی می‌شود
   
10. Reports - به‌روزرسانی گزارشات
    ├─ Dashboard به‌روزرسانی می‌شود
    ├─ گزارشات فروش به‌روزرسانی می‌شوند
    └─ گزارشات موجودی به‌روزرسانی می‌شوند
```

---

## 🗄️ ساختار دیتابیس (MongoDB Collections)

### Collections اصلی:

1. **Orders & Sales**:
   - `orders` - سفارشات عمومی
   - `dine_in_orders` - سفارشات حضوری
   - `takeaway_orders` - سفارشات بیرون‌بر
   - `delivery_orders` - سفارشات ارسال
   - `table_orders` - سفارشات میز
   - `kitchen_orders` - سفارشات آشپزخانه

2. **Inventory**:
   - `inventory_items` - اطلاعات کالاها
   - `inventory_balance` - موجودی واقعی (item × warehouse)
   - `inventory_reservations` - موجودی رزرو شده
   - `stock_movements` - حرکات کالا
   - `stock_alerts` - هشدارهای موجودی
   - `item_ledger` - دفتر کل موجودی
   - `fifo_layers` - لایه‌های FIFO
   - `warehouses` - انبارها
   - `transfers` - انتقالات

3. **Accounting**:
   - `invoices` - فاکتورها
   - `purchases` - خریدها
   - `receipts_payments` - دریافت و پرداخت
   - `bank_accounts` - حساب‌های بانکی
   - `cheques` - چک‌ها
   - `cash_flow` - جریان نقدی
   - `cashier_sessions` - جلسات صندوق

4. **Customers**:
   - `customers` - مشتریان
   - `customer_loyalties` - برنامه وفاداری
   - `customer_feedback` - نظرات

5. **Setup**:
   - `branches` - شعبه‌ها
   - `cash_registers` - صندوق‌ها
   - `menu_items` - آیتم‌های منو
   - `people` - اشخاص (مشتریان، تامین‌کنندگان)
   - `staff` - کارکنان

6. **Reports**:
   - `financial_reports_pnl` - گزارشات سود و زیان
   - (سایر گزارشات به صورت real-time محاسبه می‌شوند)

---

## 🔐 امنیت و دسترسی

### Authentication:
- سیستم احراز هویت با `/api/auth/login`
- استفاده از `AuthProvider` برای مدیریت session
- محافظت از صفحات با middleware

### Authorization:
- سیستم نقش‌ها و دسترسی‌ها در `/settings/user-roles`
- هر کاربر می‌تواند دسترسی‌های مختلف داشته باشد

---

## 📊 Dashboard (داشبورد مدیریتی)

**صفحه**: `/` (صفحه اصلی)

**ویژگی‌ها**:
- آمار کلی (فروش امروز، سود ناخالص، مشتریان فعال)
- نمودار فروش (روزانه/ماهانه/سالانه)
- نمودار روش‌های پرداخت
- پرفروش‌ترین آیتم‌های منو
- فاکتورهای اخیر
- چک‌های اخیر
- اعلان‌ها
- عملیات سریع

**Auto-refresh**: هر 2 دقیقه به‌روزرسانی می‌شود

---

## 🚀 نحوه استفاده از سیستم

### مرحله 1: راه‌اندازی اولیه
1. وارد `/onboarding` شوید
2. شعبه و صندوق را تعریف کنید
3. منو و کالاها را اضافه کنید
4. مشتریان و کارکنان را ثبت کنید
5. موجودی اولیه را وارد کنید

### مرحله 2: شروع کار روزانه
1. جلسه صندوق را باز کنید (`/operations/close-cashier`)
2. سفارشات را در POS ثبت کنید (`/pos/dine-in`)
3. آشپزخانه سفارشات را آماده می‌کند (`/pos/kitchen-orders`)
4. مشتریان پرداخت می‌کنند
5. در پایان روز صندوق را ببندید

### مرحله 3: گزارش‌گیری
1. از Dashboard برای مشاهده آمار کلی استفاده کنید
2. گزارشات تفصیلی را از `/reports` مشاهده کنید
3. گزارشات موجودی را از `/inventory/reports` بررسی کنید

---

## ⚡ ویژگی‌های پیشرفته

### 1. سیستم رزرو موجودی
- موجودی در زمان ثبت سفارش رزرو می‌شود
- موجودی واقعی فقط در زمان پرداخت کم می‌شود
- جلوگیری از overselling

### 2. FIFO برای قیمت تمام‌شده
- محاسبه دقیق قیمت تمام‌شده با روش FIFO
- لایه‌بندی موجودی بر اساس تاریخ ورود

### 3. هشدارهای خودکار موجودی
- هشدار کمبود موجودی
- هشدار موجودی منقضی
- هشدار موجودی بیش از حد

### 4. برنامه وفاداری مشتریان
- امتیازدهی خودکار
- Tier بندی (Bronze, Silver, Gold)
- استفاده از امتیازها

### 5. مدیریت چند شعبه
- پشتیبانی از چند شعبه
- گزارشات جداگانه برای هر شعبه
- انتقال بین شعبه‌ها

---

## 🔧 API Endpoints اصلی

### Orders:
- `GET/POST /api/orders` - مدیریت سفارشات
- `GET/POST /api/dine-in-orders` - سفارشات حضوری
- `GET/POST /api/takeaway-orders` - سفارشات بیرون‌بر
- `GET/POST /api/delivery-orders` - سفارشات ارسال
- `PATCH /api/orders/status` - تغییر وضعیت سفارش

### Inventory:
- `GET/POST /api/inventory-items` - مدیریت کالاها
- `GET /api/inventory/balance` - موجودی واقعی
- `POST /api/inventory/stock-movements` - ثبت حرکت کالا
- `GET /api/stock-alerts` - هشدارهای موجودی

### Accounting:
- `GET/POST /api/invoices` - فاکتورها
- `GET/POST /api/receipts-payments` - دریافت و پرداخت
- `GET /api/balance-sheet` - ترازنامه
- `GET /api/cash-flow` - جریان نقدی

### Reports:
- `GET /api/dashboard` - داده‌های داشبورد
- `GET /api/sales-reports` - گزارشات فروش
- `GET /api/inventory-reports` - گزارشات موجودی
- `GET /api/financial-reports/pnl` - سود و زیان

---

## 📝 نکات مهم

1. **موجودی واقعی**: همیشه از `inventory_balance` برای موجودی واقعی استفاده کنید
2. **رزرو موجودی**: موجودی در زمان ثبت سفارش رزرو می‌شود، نه مصرف
3. **مصرف موجودی**: موجودی فقط در زمان پرداخت سفارش مصرف می‌شود
4. **FIFO**: برای محاسبه دقیق قیمت تمام‌شده از FIFO استفاده می‌شود
5. **Auto-refresh**: بسیاری از صفحات به صورت خودکار به‌روزرسانی می‌شوند
6. **Real-time Reports**: گزارشات به صورت real-time از دیتابیس محاسبه می‌شوند

---

## 🎯 خلاصه

این سیستم یک **سیستم مدیریت رستوران کامل** است که:
- ✅ تمام عملیات رستوران را مدیریت می‌کند
- ✅ موجودی را به صورت دقیق ردیابی می‌کند
- ✅ حسابداری کامل دارد
- ✅ گزارشات جامع ارائه می‌دهد
- ✅ از تکنولوژی‌های مدرن استفاده می‌کند
- ✅ UI/UX حرفه‌ای دارد

**وضعیت کلی**: ✅ **95% تکمیل شده** و آماده استفاده

---

**تاریخ به‌روزرسانی**: به‌روز  
**نسخه**: 1.0.0

