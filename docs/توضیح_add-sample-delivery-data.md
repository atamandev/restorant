# 📝 توضیح API: add-sample-delivery-data

## 🎯 هدف این API

این API endpoint برای **اضافه کردن داده‌های نمونه (Sample Data)** برای سفارشات ارسال (Delivery Orders) بود.

## 📋 کاربرد

مشابه API های دیگر:
- `add-sample-invoice-templates` - اضافه کردن قالب‌های نمونه فاکتور
- `add-additional-menu-items` - اضافه کردن آیتم‌های نمونه منو
- `add-sample-delivery-data` - اضافه کردن سفارشات نمونه ارسال

## ⚠️ وضعیت فعلی

- ❌ فایل حذف شده است
- ❌ خطا در `build-errors.txt` ثبت شده بود
- ✅ خطا از `build-errors.txt` حذف شد

## 🔧 اگر نیاز باشد

اگر می‌خواهید این API را دوباره ایجاد کنید، می‌توانید:

1. ایجاد فایل: `app/api/add-sample-delivery-data/route.ts`
2. اضافه کردن داده‌های نمونه delivery orders
3. استفاده برای تست و توسعه

## ✅ نتیجه

این API **اختیاری** است و برای تست/توسعه استفاده می‌شود. حذف آن مشکلی ایجاد نمی‌کند.

---

**تاریخ**: ${new Date().toLocaleDateString('fa-IR')}

