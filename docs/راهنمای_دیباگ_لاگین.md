# 🔍 راهنمای دیباگ لاگین

## مشکل: وقتی دکمه ورود را می‌زنم، چیزی اتفاق نمی‌افتد

### مراحل بررسی:

1. **باز کردن Console مرورگر** (F12)
2. **لاگین را امتحان کنید** با:
   - نام کاربری: `admin`
   - رمز عبور: `123456`

3. **بررسی Console Logs**:
   - باید این پیام‌ها را ببینید:
     - `Login attempt: {username: "admin", password: "..."}`
     - `Login response status: 200`
     - `Login response data: {success: true, ...}`
     - `Login successful, setting user data`
     - `Redirecting to: /`

4. **اگر خطا دیدید**:
   - خطا را کپی کنید
   - بررسی کنید که API درست کار می‌کند

### بررسی API:

1. **باز کردن Network Tab** در DevTools
2. **لاگین را امتحان کنید**
3. **بررسی Request به `/api/auth/login`**:
   - Status باید `200` باشد
   - Response باید `{success: true, ...}` باشد

### مشکلات احتمالی:

1. **API کار نمی‌کند**: بررسی کنید که سرور در حال اجرا است
2. **Middleware مانع می‌شود**: بررسی کنید که `/api/auth/login` در middleware اجازه دارد
3. **Cookie تنظیم نمی‌شود**: بررسی کنید که `credentials: 'include'` در fetch وجود دارد
4. **Redirect کار نمی‌کند**: بررسی کنید که `window.location.href` درست کار می‌کند

---

**تاریخ**: ${new Date().toLocaleDateString('fa-IR')}

