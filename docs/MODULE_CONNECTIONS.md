# 🔗 ارتباط ماژول‌های سیستم

## 📋 فهرست
1. [نحوه اتصال ماژول‌ها](#نحوه-اتصال-ماژول‌ها)
2. [Setup (پایه)](#1-setup-پایه)
3. [POS (مرکز رویدادها)](#2-pos-مرکز-رویدادها)
4. [Kitchen (آشپزخانه)](#3-kitchen-آشپزخانه)
5. [Inventory (انبارداری)](#4-inventory-انبارداری)
6. [Accounting (حسابداری)](#5-accounting-حسابداری)
7. [Customers (CRM)](#6-customers-crm)
8. [Reports (گزارشات)](#7-reports-گزارشات)
9. [Settings (تنظیمات)](#8-settings-تنظیمات)
10. [مثال کامل: فروش یک پیتزا](#مثال-کامل-فروش-یک-پیتزا)

---

## نحوه اتصال ماژول‌ها

```
Setup (پایه)
    ↓
    ├──→ POS
    ├──→ Operations
    └──→ Reports

POS (مرکز رویدادها)
    ↓
    ├──→ Kitchen (ارسال سفارش)
    ├──→ Accounting (ثبت فاکتور)
    ├──→ Inventory (کاهش موجودی)
    └──→ Customers (به‌روزرسانی امتیاز)

Kitchen
    ↓
    └──→ POS (اعلان آماده بودن)

Inventory
    ↓
    ├──← Accounting (کاهش موجودی)
    ├──← POS (کاهش موجودی)
    └──→ Reports (گزارش موجودی)

Accounting
    ↓
    ├──← POS (ثبت فاکتور)
    ├──← Inventory (ثبت خرید)
    └──→ Reports (گزارشات مالی)

Customers
    ↓
    ├──← POS (به‌روزرسانی امتیاز)
    └──→ Reports (گزارش مشتریان)

Reports
    ↓
    ├──← POS
    ├──← Accounting
    ├──← Inventory
    └──← Customers

Settings
    ↓
    └──→ همه ماژول‌ها (دسترسی و تنظیمات)
```

---

## 1️⃣ Setup (پایه)

**نقش**: اطلاعات پایه که همه ماژول‌ها از آن استفاده می‌کنند

**APIها**:
- `/api/branches` - مدیریت شعبه‌ها
- `/api/cash-registers` - مدیریت صندوق‌ها
- `/api/tax-rates` - نرخ مالیات
- `/api/menu-items` - آیتم‌های منو
- `/api/inventory-items` - مواد اولیه

**وابستگی‌ها**: هیچ (پایه است)

**استفاده توسط**:
- ✅ POS → برای `branchId`, `cashRegisterId`, `menuItems`, `taxRate`
- ✅ Operations → برای `branchId`, `cashRegisterId`
- ✅ Reports → برای فیلتر کردن بر اساس `branchId`

---

## 2️⃣ POS (مرکز رویدادها)

**نقش**: مرکز رویدادها - همه چیز از فروش شروع می‌شود

**APIها**:
- `/api/dine-in-orders` - سفارشات حضوری
- `/api/takeaway-orders` - سفارشات بیرون‌بر
- `/api/delivery-orders` - سفارشات ارسال
- `/api/quick-sales` - فروش سریع

**وابستگی‌ها**:
- ✅ Setup → برای `branchId`, `registerId`, `menuItems`, `taxRate`

**Trigger می‌کند**:
- ✅ Kitchen → ارسال سفارش برای پخت
- ✅ Accounting → ثبت فاکتور فروش (وقتی پرداخت شد)
- ✅ Inventory → کاهش موجودی مواد اولیه (بر اساس recipe)
- ✅ Customers → به‌روزرسانی امتیاز وفاداری (اگر customerId وجود دارد)

**جریان کار**:
```
POS ثبت سفارش
    ↓
اگر status = 'paid':
    ├──→ Accounting (create invoice)
    ├──→ Inventory (deduct stock)
    └──→ Customers (update loyalty)
```

---

## 3️⃣ Kitchen (آشپزخانه)

**نقش**: دریافت سفارش از POS و اعلام آماده بودن

**APIها**:
- `/api/kitchen-orders` - مدیریت سفارشات آشپزخانه
- `/api/kitchen-orders/[id]` - به‌روزرسانی وضعیت

**وابستگی‌ها**:
- ✅ POS → دریافت سفارش

**Trigger می‌کند**:
- ✅ POS → اعلان آماده بودن (status: 'ready')
- ✅ Inventory → اگر سفارش لغو شد، موجودی برگردانده می‌شود

**جریان کار**:
```
POS → Kitchen (ارسال سفارش)
    ↓
Kitchen (status: 'preparing')
    ↓
Kitchen (status: 'ready')
    ↓
POS (اعلان به مشتری)
```

---

## 4️⃣ Inventory (انبارداری)

**نقش**: مدیریت موجودی مواد اولیه

**APIها**:
- `/api/inventory-items` - مدیریت موجودی
- `/api/item-ledger` - دفتر کل موجودی
- `/api/transfers` - انتقال بین انبارها
- `/api/stock-alerts` - هشدارهای کمبود

**وابستگی‌ها**:
- ✅ Accounting → کاهش موجودی هنگام فروش (از فاکتور)
- ✅ POS → کاهش موجودی هنگام فروش (بر اساس recipe)
- ✅ Purchases → افزایش موجودی هنگام خرید

**Trigger می‌کند**:
- ✅ Stock Alerts → ایجاد هشدار اگر موجودی کم شد
- ✅ Reports → گزارشات موجودی

**جریان کار**:
```
Accounting (فاکتور فروش) → Inventory (کاهش موجودی)
    ↓
Inventory (بررسی موجودی)
    ↓
اگر موجودی < minStock:
    └──→ Stock Alerts (ایجاد هشدار)
```

---

## 5️⃣ Accounting (حسابداری)

**نقش**: مدیریت مالی و تراکنش‌ها

**APIها**:
- `/api/invoices` - فاکتورهای فروش و خرید
- `/api/receipts-payments` - دریافت و پرداخت
- `/api/cheques` - چک‌ها
- `/api/financial-reports` - گزارشات مالی
- `/api/cash-flow` - جریان نقدی

**وابستگی‌ها**:
- ✅ POS → ثبت فاکتور فروش
- ✅ Purchases → ثبت فاکتور خرید
- ✅ Inventory → هزینه مواد اولیه (برای COGS)

**Trigger می‌کند**:
- ✅ Inventory → کاهش موجودی (وقتی فاکتور فروش ثبت شد)
- ✅ Receipts/Payments → ثبت تراکنش مالی
- ✅ Reports → گزارشات مالی

**جریان کار**:
```
POS (پرداخت شد) → Accounting (ثبت فاکتور)
    ↓
Accounting (ثبت فاکتور)
    ↓
├──→ Inventory (کاهش موجودی)
└──→ Receipts/Payments (ثبت تراکنش)
```

---

## 6️⃣ Customers (CRM)

**نقش**: مدیریت اطلاعات مشتریان

**APIها**:
- `/api/customers` - مدیریت مشتریان
- `/api/customer-loyalties` - باشگاه مشتریان
- `/api/customer-feedback` - نظرات مشتریان

**وابستگی‌ها**:
- ✅ POS → به‌روزرسانی امتیاز وفاداری (بعد از پرداخت)

**Trigger می‌کند**:
- ✅ Reports → گزارشات مشتریان وفادار

**جریان کار**:
```
POS (پرداخت با customerId) → Customers (به‌روزرسانی امتیاز)
    ↓
Customers (محاسبه امتیاز)
    ↓
├──→ Loyalty Tiers (بروزرسانی tier)
└──→ Reports (گزارش مشتریان وفادار)
```

---

## 7️⃣ Reports (گزارشات)

**نقش**: جمع‌آوری و تحلیل داده‌ها

**APIها**:
- `/api/dashboard` - داشبورد مدیریتی
- `/api/sales-reports` - گزارشات فروش
- `/api/inventory-reports` - گزارشات موجودی
- `/api/customer-supplier-reports` - گزارشات مشتری-تامین‌کننده
- `/api/financial-reports` - گزارشات مالی

**وابستگی‌ها**:
- ✅ همه ماژول‌ها → برای جمع‌آوری داده

**Trigger نمی‌کند** (فقط خواننده است)

**جریان کار**:
```
Reports (درخواست گزارش)
    ↓
├──→ POS (خواندن سفارشات)
├──→ Accounting (خواندن فاکتورها)
├──→ Inventory (خواندن موجودی)
└──→ Customers (خواندن اطلاعات مشتریان)
    ↓
Reports (تحلیل و نمایش)
```

---

## 8️⃣ Settings (تنظیمات)

**نقش**: کنترل دسترسی و تنظیمات

**APIها**:
- `/api/user-roles` - مدیریت نقش‌ها و مجوزها
- `/api/printer-config` - تنظیمات چاپگر
- `/api/invoice-templates` - قالب‌های فاکتور
- `/api/backup-restore` - پشتیبان‌گیری
- `/api/audit-log` - لاگ فعالیت‌ها
- `/api/settings` - تنظیمات مرکزی

**وابستگی‌ها**: هیچ (مستقل است)

**استفاده توسط**:
- ✅ همه ماژول‌ها → برای بررسی دسترسی (Authorization)

**جریان کار**:
```
User درخواست API می‌دهد
    ↓
Settings (بررسی دسترسی)
    ↓
اگر دسترسی دارد:
    └──→ ماژول مورد نظر (اجرای درخواست)
اگر دسترسی ندارد:
    └──→ 403 Forbidden
```

---

## مثال کامل: فروش یک پیتزا

### سناریو: فروش یک پیتزای مخصوص به قیمت 150,000 تومان

#### 1️⃣ Setup (پایه)
```
POS از Setup می‌خواند:
- branchId: "branch_001"
- cashRegisterId: "register_001"
- menuItem: { id: "pizza_makhsoos", price: 150000, recipe: [...] }
- taxRate: 9%
```

#### 2️⃣ POS (ثبت سفارش)
```http
POST /api/dine-in-orders
{
  "tableNumber": 5,
  "items": [{ "menuItemId": "pizza_makhsoos", "quantity": 1 }],
  "branchId": "branch_001",
  "customerId": "customer_123"
}
```
**نتیجه**: سفارش ثبت شد → `orderId: "order_456"`

#### 3️⃣ Kitchen (ارسال سفارش)
```http
POST /api/kitchen-orders
{
  "orderId": "order_456",
  "items": [{ "name": "پیتزای مخصوص", "quantity": 1 }],
  "status": "preparing"
}
```
**نتیجه**: آشپز شروع به پخت کرد

#### 4️⃣ Kitchen (آماده شدن)
```http
PUT /api/kitchen-orders/kitchen_789
{
  "status": "ready"
}
```
**نتیجه**: POS به مشتری اعلان داد که غذا آماده است

#### 5️⃣ POS (پرداخت)
```http
PUT /api/dine-in-orders/order_456
{
  "status": "paid",
  "paymentMethod": "cash",
  "paidAmount": 163500
}
```
**نتیجه**: مشتری پرداخت کرد → **Trigger: Accounting, Inventory, Customers**

#### 6️⃣ Accounting (ثبت فاکتور)
```http
POST /api/invoices
{
  "type": "sales",
  "orderId": "order_456",
  "totalAmount": 163500,
  "paymentMethod": "cash",
  "status": "paid"
}
```
**نتیجه**: فاکتور ثبت شد → `invoiceId: "invoice_789"` → **Trigger: Inventory**

#### 7️⃣ Inventory (کاهش موجودی)
```http
POST /api/item-ledger
{
  "documentType": "sale",
  "documentId": "invoice_789",
  "items": [
    { "itemId": "dough", "quantityOut": 250 },
    { "itemId": "cheese", "quantityOut": 100 },
    { "itemId": "tomato", "quantityOut": 50 }
  ]
}
```
**نتیجه**: موجودی مواد اولیه کم شد

#### 8️⃣ Customers (به‌روزرسانی امتیاز)
```http
PUT /api/customer-loyalties/customer_123
{
  "totalPoints": 1163,
  "pointsEarned": 163,
  "currentTier": "Silver"
}
```
**نتیجه**: امتیاز مشتری به‌روزرسانی شد

#### 9️⃣ Reports (ثبت در گزارشات)
```http
GET /api/dashboard
GET /api/sales-reports?reportType=summary
GET /api/reports/top-menu-items
```
**نتیجه**: داشبورد به‌روزرسانی شد

#### 🔟 Operations (بستن صندوق - پایان روز)
```http
POST /api/cashier-sessions/session_001/close
{
  "actualCashAmount": 500000,
  "actualCardAmount": 200000
}
```
**نتیجه**: صندوق بسته شد و موجودی نقد تطبیق داده شد

---

## خلاصه

| ماژول | نقش | وابستگی | Trigger می‌کند |
|-------|-----|---------|----------------|
| **Setup** | پایه | هیچ | POS, Operations |
| **POS** | مرکز رویدادها | Setup | Kitchen, Accounting, Inventory, Customers |
| **Kitchen** | پخت | POS | POS (when ready) |
| **Inventory** | موجودی | Accounting, POS | Stock Alerts, Reports |
| **Accounting** | مالی | POS, Purchases | Inventory, Reports |
| **Customers** | CRM | POS | Reports |
| **Reports** | تحلیل | همه | هیچ |
| **Settings** | کنترل | هیچ | همه (Authorization) |

**نکته مهم**: از نظر کاربر، همه اینها یک کلیک است (ثبت سفارش → پرداخت)، اما در پشت صحنه ده‌ها API از ماژول‌های مختلف هماهنگ کار می‌کنند! 🎯

