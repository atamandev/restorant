# 🔗 لینک‌های Localhost برای بخش‌های ناقص

**پورت پیش‌فرض**: `3000`  
**Base URL**: `http://localhost:3000`

---

## ⚠️ بخش‌های ناقص (3 مورد)

### 1️⃣ **Upload تصاویر** ⚠️

**لینک صفحه**: 
```
http://localhost:3000/settings/restaurant
```

**مسیر فایل**: `app/settings/restaurant/page.tsx`  
**خط مشکل**: 270

**مشکل**: 
- TODO برای آپلود تصویر به سرور
- فعلاً فقط data URL ذخیره می‌شود

**برای تست**:
1. به لینک بالا بروید
2. بخش "لوگو رستوران" یا "تصویر" را پیدا کنید
3. سعی کنید تصویری آپلود کنید
4. ببینید که فقط data URL ذخیره می‌شود (نه URL واقعی)

---

### 2️⃣ **Printer Integration** ⚠️ (نیاز به تست)

**لینک صفحه**: 
```
http://localhost:3000/settings/printer-config
```

**مسیر فایل**: `app/settings/printer-config/page.tsx`

**مشکل**: 
- API کامل است اما نیاز به تست با چاپگر واقعی
- نیاز به تنظیمات port و driver

**برای تست**:
1. به لینک بالا بروید
2. تنظیمات چاپگر را پیکربندی کنید
3. با چاپگر واقعی تست کنید
4. ببینید که آیا چاپ انجام می‌شود یا نه

**API مربوطه**:
```
http://localhost:3000/api/printer-config
```

---

### 3️⃣ **Authorization Middleware** ⚠️ (نیاز به تست کامل)

**مسیر فایل**: `middleware.ts` (در root پروژه)

**مشکل**: 
- Authentication کامل نیست
- فعلاً همه routes اجازه دسترسی دارند

**برای تست**:
1. بدون لاگین به صفحات مختلف بروید:
   ```
   http://localhost:3000/dashboard
   http://localhost:3000/pos
   http://localhost:3000/accounting
   http://localhost:3000/settings
   ```
2. ببینید که آیا دسترسی محدود شده یا نه
3. تست کنید که آیا middleware کار می‌کند یا نه

**صفحه لاگین**:
```
http://localhost:3000/login
```

---

## 📋 لینک‌های مرتبط برای تست

### صفحات اصلی:
```
http://localhost:3000/                    # Dashboard
http://localhost:3000/login               # ورود
http://localhost:3000/pos                 # POS
http://localhost:3000/accounting         # حسابداری
http://localhost:3000/inventory           # انبار
http://localhost:3000/customers           # مشتریان
http://localhost:3000/reports             # گزارشات
http://localhost:3000/settings            # تنظیمات
```

### API Endpoints مرتبط:
```
http://localhost:3000/api/restaurant-settings    # تنظیمات رستوران
http://localhost:3000/api/printer-config         # تنظیمات چاپگر
http://localhost:3000/api/auth/login              # لاگین
```

---

## 🔧 راهنمای تست

### تست Upload تصاویر:

1. **باز کردن صفحه**:
   ```
   http://localhost:3000/settings/restaurant
   ```

2. **پیدا کردن بخش آپلود تصویر**:
   - به بخش "لوگو رستوران" یا "تصویر" بروید
   - دکمه "آپلود تصویر" را پیدا کنید

3. **تست آپلود**:
   - یک تصویر انتخاب کنید
   - ببینید که آیا به سرور آپلود می‌شود یا فقط data URL ذخیره می‌شود
   - در Console مرورگر خطاها را بررسی کنید

4. **بررسی کد**:
   - فایل `app/settings/restaurant/page.tsx` خط 270 را بررسی کنید
   - ببینید که TODO وجود دارد

---

### تست Printer Integration:

1. **باز کردن صفحه**:
   ```
   http://localhost:3000/settings/printer-config
   ```

2. **تنظیمات چاپگر**:
   - تنظیمات چاپگر را پیکربندی کنید
   - Port و Driver را تنظیم کنید

3. **تست چاپ**:
   - یک فاکتور یا سفارش را چاپ کنید
   - ببینید که آیا چاپ انجام می‌شود یا نه

4. **بررسی API**:
   ```
   http://localhost:3000/api/printer-config
   ```

---

### تست Authorization:

1. **تست بدون لاگین**:
   - مرورگر را در حالت Incognito باز کنید
   - به صفحات مختلف بروید:
     ```
     http://localhost:3000/dashboard
     http://localhost:3000/pos
     http://localhost:3000/accounting
     ```
   - ببینید که آیا دسترسی داده می‌شود یا redirect به login می‌شود

2. **تست با لاگین**:
   - به `http://localhost:3000/login` بروید
   - لاگین کنید
   - ببینید که آیا دسترسی‌ها درست کار می‌کنند

3. **بررسی Middleware**:
   - فایل `middleware.ts` را بررسی کنید
   - ببینید که آیا authentication کامل است یا نه

---

## 📝 نکات مهم

1. **قبل از تست**:
   - مطمئن شوید که سرور در حال اجرا است:
     ```bash
     npm run dev
     ```
   - سرور باید روی `http://localhost:3000` اجرا شود

2. **برای بررسی خطاها**:
   - Console مرورگر را باز کنید (F12)
   - Network tab را بررسی کنید
   - ببینید که آیا درخواست‌های API موفق هستند یا نه

3. **برای رفع مشکلات**:
   - به فایل‌های مربوطه بروید
   - کد را بررسی کنید
   - TODO ها را پیدا کنید و رفع کنید

---

**تاریخ**: ${new Date().toLocaleDateString('fa-IR')}

